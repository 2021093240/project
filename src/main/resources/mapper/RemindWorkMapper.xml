<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.RemindWorkMapper">

    <insert id="addRemindWork" useGeneratedKeys="true" keyProperty="id">

        insert into
            remind (remind, t_id,teacher_name)
        values
            (#{remind}, #{tId},#{teacherName})

    </insert>


</mapper>


